apiVersion: v1
kind: Service
metadata:
  name: nodalarc-ome
  namespace: {{ .Values.namespace }}
  labels:
    app: nodalarc-ome
spec:
  selector:
    app: nodalarc-ome
  ports:
    - name: health
      port: 8081
      targetPort: 8081
      protocol: TCP
