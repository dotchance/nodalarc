# Leave blank — runtime config.js or browser hostname fallback handles URL resolution.
# Do NOT set K8s-internal DNS names here — they are unreachable from browsers.
VITE_VSAPI_WS_URL=
VITE_VSAPI_REST_URL=
