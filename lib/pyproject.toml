[project]
name = "nodalarc"
version = "0.1.0"
description = "Nodal Arc shared library - Pydantic models, DB schema, ZeroMQ channels"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.0",
]
